\todo{replace dummy code with correct code}
\cite{hal-02158423}
\begin{algorithm}
    \caption{\textbf{Retargeting Schritt} t Vor Rendern Frame t+1 nach Sortier Schritt}
    \begin{algorithmic}[1]
        \STATE //permutation indices from precomputed texture
        \STATE List<PixelPermutation> L = getRetargetedSeedsIndices(incomingSeeds); 
        \FORALL{i = 1 .. numberOfPixelsPerBlock}
        \STATE $retargetedSeeds(L.getPermIndices()) = incomingSeeds(L.getOldIndices());$
        \ENDFOR
    \end{algorithmic}
    \label{alg:retargeting}
\end{algorithm}